# Branch Map — project branch hierarchy and routing config
# Location: .claude/branch-map.yaml (auto-generated by /branch-init)
#
# Edit this file to match your project's branching model.
# Claude reads this before creating branches, merging, or routing work items.
# GitHub Actions reads this for auto-sync (hub-and-spoke merge flow).

version: 1

# Branch hierarchy: ordered from trunk to working parent
# Claude derives merge targets from this chain.
trunk_chain:
  - main
  # - develop        # uncomment if you use a develop branch
  # - research       # uncomment for deeper chains

# The branch that feature branches are based on.
# Must be the last entry in trunk_chain.
# Acts as the "hub" — all features merge UP here, then cascade DOWN.
working_parent: main

# Branch naming
branch_rules:
  feature_prefix: feat    # feat/FEAT-001-slug
  default_merge_target: main  # must match working_parent

# Feature/integration branches and their relationships (OPTIONAL)
# Without this section, ALL remote feature branches auto-sync through working_parent.
# Add entries only to override: custom parent, merge_target, worktree, or role.
# branches:
#   feature-docs:
#     parent: main
#     merge_target: main
#     worktree: /path/to/Project-Docs
#     role: docs
#     paths: [docs/**, mkdocs.yml, README.md]
#
#   feature-backend:
#     parent: main
#     merge_target: main
#     worktree: /path/to/Project-Backend
#     role: backend
#     paths: [src/**, lib/**, tests/**]
#
#   feature-frontend:
#     parent: feature-backend
#     merge_target: feature-backend
#     worktree: /path/to/Project-Frontend
#     role: frontend
#     paths: [web/**, ui/**]

# Sync rules — defines cascade triggers (optional, for documentation)
# The GHA workflow derives sync from branches[].parent automatically.
# sync:
#   - from: feature-docs
#     to: [feature-backend, feature-frontend]
#     trigger: docs/, mkdocs.yml

# Merge policy
merge_policy:
  require_parent_freshness: true   # branch must be up-to-date with parent before merge
  require_green_ci: true           # CI checks must pass before merge
  ask_confirm_before_merge: true   # ask user before executing merge
  delete_branch_after_merge: true  # clean up feature branch after merge
